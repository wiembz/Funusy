{% extends 'BACK/base.html.twig' %}

{% block title %}Edit Commentaire{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Edit Commentaire</h3>
        </div>
        <div class="card-body">
            {% include 'commentaire/_form.html.twig' with {'button_label': 'Update'} %}
        </div>
        <div class="card-footer">
            <a href="{{ path('app_commentaire_index') }}" class="btn btn-secondary">Back to List</a>
        </div>
    </div>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
