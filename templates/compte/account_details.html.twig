<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Details</title>
    <style>
        .logo {
            float: left;
            margin-right: 20px;
        }
    </style>
</head>
<body>
    <div class="logo">
        <img src="/BACK/assets/img/favicon/Asset.png" alt="Logo">
    </div>
    <h1>Account Details</h1>
    <dl>
        <dt>Rib:</dt>
        <dd>{{ compte.getRib() }}</dd>
        
        <dt>Solde:</dt>
        <dd>{{ compte.getSolde() }}</dd>
        
        <dt>Date d'Ouverture:</dt>
        <dd>{{ compte.getDateOuverture() ? compte.getDateOuverture()|date('Y-m-d') : '' }}</dd>
        
        <dt>Type de Compte:</dt>
        <dd>{{ compte.getTypeCompte() }}</dd>
        
        <dt>ID Utilisateur:</dt>
        <dd>{{ compte.getIdUser() }}</dd>
    </dl>
</body>
</html>