<form method="post" action="{{ path('app_carte_bancaire_delete', {'numCarte': carte_bancaire.numCarte}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte_bancaire.numCarte) }}">
    <button class="btn">Delete</button>
</form>
