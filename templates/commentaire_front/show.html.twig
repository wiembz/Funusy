<!DOCTYPE html>

<title>Commentaire</title>

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCommentaire</th>
                <td>{{ commentaire.idCommentaire }}</td>
            </tr>
            <tr>
                <th>Contenue</th>
                <td>{{ commentaire.contenue }}</td>
            </tr>
            <tr>
                <th>DateCommentaire</th>
                <td>{{ commentaire.dateCommentaire ? commentaire.dateCommentaire|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_front_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_front_edit', {'idCommentaire': commentaire.idCommentaire}) }}">edit</a>

    {{ include('commentaire_front/_delete_form.html.twig') }}
{% endblock %}
