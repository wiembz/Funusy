<!DOCTYPE html>

<title>Edit Commentaire</title>

{% block body %}
    <h1>Edit Commentaire</h1>

    {{ include('commentaire_front/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_commentaire_front_index') }}">back to list</a>

    {{ include('commentaire_front/_delete_form.html.twig') }}
{% endblock %}
