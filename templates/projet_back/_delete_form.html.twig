<form method="post" action="{{ path('app_projet_back_delete', {'idProjet': projet.idProjet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projet.idProjet) }}">
    <button class="btn">Delete</button>
</form>
