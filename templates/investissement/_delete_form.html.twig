<form method="post" action="{{ path('app_investissement_delete', {'idInvestissement': investissement.idInvestissement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ investissement.idInvestissement) }}">
    <button class="btn">Delete</button>
</form>
