<form method="post" action="{{ path('app_agence_front_delete', {'codeAgence': agence.codeAgence}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agence.codeAgence) }}">
    <button class="btn">Delete</button>
</form>
