<!DOCTYPE html>

<title>Agence</title>

{% block body %}
    <h1>Agence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CodeAgence</th>
                <td>{{ agence.codeAgence }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ agence.adresse }}</td>
            </tr>
            <tr>
                <th>Codepostal</th>
                <td>{{ agence.codepostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_agence_front_index') }}">back to list</a>

    <a href="{{ path('app_agence_front_edit', {'codeAgence': agence.codeAgence}) }}">edit</a>

    {{ include('agence_front/_delete_form.html.twig') }}
{% endblock %}
