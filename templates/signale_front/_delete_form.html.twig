<form method="post" action="{{ path('app_signale_front_delete', {'idSignal': signale.idSignal}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signale.idSignal) }}">
    <button class="btn">Delete</button>
</form>
